import numpy as np

# Создание массива

array = np.array([1, 2, 3, 4, 5])

# Выполнение математических операций

array_sum = np.sum(array)  # Сумма элементов

array_mean = np.mean(array)  # Среднее значение
print(array_sum)
print(array_mean)